# İletişim formu başvurularını kalıcı hale getirmek için (opsiyonel).
# Tanımlıysa başvurular Neon Postgres'te `void_leads` tablosuna yazılır;
# tanımlı değilse yerel `data/void-leads.json` dosyasına düşer.
# DATABASE_URL="postgres://kullanici:sifre@host/db?sslmode=require"
